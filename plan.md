## Phong cách thiết kế

Thiết kế mang phong cách **Editorial**, **Swiss Minimalism** kết hợp cảm hứng từ các poster thời trang cao cấp (COS, Acne Studios, Jacquemus). Toàn bộ bố cục sử dụng rất nhiều khoảng trắng, typography làm yếu tố trung tâm, màu sắc tối giản và các chi tiết đồ họa dạng SVG để dẫn hướng thị giác. Nền sử dụng tông **Olive Green** (`#5A6422` hoặc `#596321`), chữ màu **Off White** (`#F5F5F0` hoặc `#FAFAF5`), viền ngoài màu tím nhạt (`#9B6DFF`, dày khoảng 2px). Font tiêu đề nên dùng **Cormorant Garamond**, **Bodoni Moda**, **Editorial New** hoặc **Canela** với kiểu serif mảnh, `font-size` khoảng **68–76px**, `font-weight: 300`, `line-height: 0.82`, `letter-spacing: -2px`, viết hoa toàn bộ. Các đoạn chữ nhỏ dùng **Inter**, **Helvetica** hoặc **Neue Haas Grotesk**, kích thước **9–10px**, `font-weight: 600`, `letter-spacing: 1px`, viết hoa. Bố cục chủ yếu sử dụng `position: relative` kết hợp các phần tử `absolute` thay vì grid truyền thống; header được căn `space-between`, tiêu đề căn giữa, các chú thích nhỏ, mũi tên và badge được đặt tự do để tạo nhịp thị giác. Các mũi tên cong, hình ngôi sao zigzag và blob phía dưới đều nên vẽ bằng **SVG** với `stroke: 1.5px`, `fill: none`, không dùng icon. Thiết kế hoàn toàn phẳng (**Flat Design**), không sử dụng shadow, gradient, blur hay animation, tạo cảm giác sang trọng, hiện đại và đậm chất tạp chí (editorial).